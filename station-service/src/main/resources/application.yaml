server:
  port: 8081

spring:

  application:
    name: station-service

  datasource:
    url: jdbc:postgresql://localhost:5432/stationServiceDb
    username: postgres
    password: 12345

  jpa:
    hibernate:
#      ddl-auto: update
       ddl-auto: create

    show-sql: true

    properties:
      hibernate:
        format_sql: true


management:
  endpoints:
    web:
      exposure:
        include: health,info