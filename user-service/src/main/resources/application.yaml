server:
  port: 8082

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/userServiceDb
    username: postgres
    password: 12345

  jpa:
    hibernate:
#      ddl-auto: update
       ddl-auto: create

    show-sql: true

    properties:
      hibernate:
        format_sql: true


management:
  endpoints:
    web:
      exposure:
        include: health,info